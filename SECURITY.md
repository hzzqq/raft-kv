# 安全政策（Security Policy）

本仓库提供一套教学/研究用途的 Raft 共识 + 分片 KV 存储实现，并配有密钥泄露静态扫描门禁
（`scripts/check_secrets.py`）。本文件定义漏洞披露流程与贡献者的安全约定。

## 支持版本

仅 `master` 分支的最新提交接受安全相关反馈。历史 tag / 未合并分支不保证修复。

## 漏洞披露（Reporting a Vulnerability）

**请勿通过公开 Issue / Pull Request 披露安全漏洞。**

请通过以下任一**私下**渠道报告：

- 邮件至维护者（见 `README.md` 或仓库主页 Profile 中的安全邮箱）；
- GitHub 仓库的 **Security → Report a vulnerability**（若已启用私有漏洞上报）；
- 或在本仓库开启 **private vulnerability report**。

报告时请尽量包含：

1. 漏洞类型与受影响组件（`raft` / `kvraft` / `shardkv` / `shardmaster` / `gateway` / `kvcli` / `transport`）；
2. 复现步骤 / PoC（最小化、可本地构建运行，无需真实凭证）；
3. 影响评估：触发条件、可达性、潜在后果（数据泄露 / 服务不可用 / 权限提升等）；
4. 建议修复方向（可选）。

## 响应时限

- **确认收悉**：收到后 5 个工作日内确认收到；
- **初步评估**：20 个工作日内给出严重级别与修复计划；
- **协调披露**：修复合并后，经报告者同意可公开致谢���若报告者希望匿名，则仅记录 CVE/公告，不公开身份。

## 范围与边界

属于安全响应范围的：

- 共识层 / KV 层 / 分片迁移中的数据损坏、状态机分歧；
- 网关 / 传输层的认证绕过、未授权访问、请求走私；
- 客户端 / 工具链的凭证泄露面（如日志中落明文 token）；
- 仓库内的密钥/凭证硬编码或误提交。

**不视为漏洞**的（教学/研究定位）：

- 单节点部署下未启用 TLS / 鉴权（本实现默认信任内网部署，非生产级传输安全）；
- 依赖 `go` 工具链版本的已知 CVE（请升级 toolchain）；
- 仅影响 Demo / 单测、无实际部署路径的问题。

## 贡献者安全约定

- 提交前 `make hooks` 已安装 `pre-commit` 门禁，包含 `check_secrets.py`：
  任何 PEM 私钥、AWS `AKIA*` 访问密钥、AWS 密钥字面量会被**硬阻断**；
  明文口令 / Slack / GitHub token 会被**告警**。请勿试图绕过（`--no-verify`）。
- 密钥一律走环境变量或外部密钥管理，禁止写入源码、`*.json`、`*.toml`、`*.yaml` 配置；
- 发现疑似密钥已进入历史提交：立即轮换凭证，并联系维护者处理历史记录（必要时走
  `git filter-repo` 重写，属破坏性操作，仅由维护者执行）。

## 安全相关配置检查清单（部署前）

- [ ] 网关 `admin` / 调试端点（如 `/debug/*`）未对公网暴露；
- [ ] 启动参数、配置文件不含明文口令；
- [ ] 本仓库 CI 的 `secrets` 门禁（CI `docs-links` job）为绿色；
- [ ] 生产部署已在外层补充 TLS / mTLS（本实现不内置传输加密）。
