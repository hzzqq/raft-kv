# Grafana 看板自动供给：从只读卷加载 deploy/grafana/dashboards/*.json。
apiVersion: 1
providers:
  - name: raftkv
    orgId: 1
    folder: ""
    type: file
    disableDeletion: true
    allowUiUpdates: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
