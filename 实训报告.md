# 分布式容错 KV 存储系统实训报告（Raft + ShardKV）

## 封面

| 项 | 内容 |
|---|---|
| 课程 | 软件工程实训（分布式系统方向，基于 MIT 6.824 实验框架） |
| 项目 | `raft-kv`（Raft 共识 + 容错 KV + 分片 KV / ShardKV） |
| 学号 | 231042Y133 |
| 姓名 | 黄子洲 |
| 技术栈 | Go 1.22，模块化 `raftkv`，零外部依赖（自研 labrpc / metrics / YAML 子集解析器） |
| 提交日期 | 2026-09-03 |

## 摘要

本报告完整记录一套 MIT 6.824 风格分布式容错 KV 存储系统（`raft-kv`）从设计、实现到工程化的全过程。系统自下而上分为共识层（Raft）、容错 KV 层、分片配置服务（ShardMaster）与分片数据面（ShardKV），并在其上构建可运行、可观测、可压测的全栈 HTTP 网关、命令行客户端与演示程序。

实训的核心技术贡献包括：（1）Raft 共识层的工程化增强——Pre-Vote 预投票、LeadershipTransfer 平滑换主、leader lease 线性一致读，以及选举抖动窗口下的"本任期已提交"读守卫（I195）；（2）ShardKV 分片迁移的若干关键不变量——双路迁移（推送+拉取）、GC-after-ack、迁移到达合并而非覆盖、配置号幂等与过期传输安全丢弃，并据此根治了长期悬而未决的"3 组 / 多跳再平衡冻结"间歇性挂死；（3）以 `self-driving-dev` 自主迭代机制驱动 200+ 轮功能增强、稳健性与可观测性建设。

验证结果：Labs 2–4 确定性测试矩阵稳定全绿（含混沌测试与 `-race` 竞态守护），全量语句覆盖率约 74%（各包明细见第 5 节测试、覆盖率与性能验证）。关键认知是——分布式系统的正确性必须靠"服务端状态机不变量 + 确定性测试 + 混沌复现"三重保障，客户端侧微优化往往是死路；而再平衡冻结这类"间歇性挂死"，唯有回到异步迁移与配置推进的竞态根因、用"只读安全回退"替代"状态改写"才能根治。

注：高压力 swing 混沌测试 `TestChaosSwingWriteDataLoss` 在隔离运行（`go test -run` 单测）下确定性通过；仅在全包并发 `go test -cover ./...`（19 个包同时抢占 CPU）时偶发触碰其内部 swing  deadline，属测试健壮性待加固项，非核心正确性格栅回归（该测试在剔除 I195 的 HEAD 版本上同样偶发，已对照验证）。

---

## 1. 项目概述

本实训从零实现一套 MIT 6.824 风格的分布式容错 KV 存储系统，依次覆盖：

- **Lab 2A–2D（Raft 共识）**：领导人选举、日志复制、持久化、快照压缩；
- **Lab 3（容错 KV 层）**：在 Raft 之上构建线性一致的 KV 服务，含客户端幂等去重与崩溃恢复；
- **Lab 4（ShardKV 分片 KV）**：多 replica group 分片、经 ShardMaster 轮转再平衡、双路分片迁移（推送+拉取）、GC-after-ack、快照压缩；
- **工程化全栈**：`cluster`（可复用内存集群 harness）→ `demo`（进程内 + HTTP 全栈演示）→ `gateway`（REST 网关 + 可观测端点）→ `kvcli`（命令行 + 压测），并以 `self-driving-dev` 自主迭代机制完成多轮功能增强、稳健性与可观测性建设。

目标是交付一套**真正能跑起来、可观测、可压测、测试矩阵稳定全绿**的分布式系统工程，而非仅通过教学用例。

---

## 2. 系统架构设计

系统采用清晰的分层结构，自下而上为：

| 层 | 包 | 职责 |
|---|---|---|
| 共识层 | `src/raft` | 选举 / 日志复制 / 持久化 / 快照，学期术语单调递增，仅提交"当前任期"日志 |
| KV 层 | `src/kvraft` | 线性一致 KV，客户端幂等去重，no-op 拉动旧任期提交 |
| 配置服务 | `src/shardmaster` | 维护 10 个分片 → replica group 映射，经 Raft 复制每次配置变更 |
| 数据面 | `src/shardkv` | 按当前配置服务本组分片，双路分片迁移，跨迁移幂等 |
| 全栈 | `cluster` / `demo` / `gateway` / `kvcli` | 可运行、可观测、可压测的上层组件 |

**数据面关键不变量**：
- 每个 replica group 仅服务当前配置下归属自身的分片集合，越界请求返回 `ErrWrongGroup` 由客户端重查配置重试；
- 分片迁移采用**旧 owner 推送 + 新 owner 拉取双路**；旧 owner 收到新 owner 的 ack（新组已 Raft 提交该分片）后才提交 `GCShard` 回收，保证对方落盘前数据不丢；
- 新 owner 在分片正式归属前可能已收到客户端写，**迁移到达时必须合并而非覆盖**（仅补旧 owner 多出的 key + 取更大 `LastSeq`），否则迁移窗口内的写入会被冲掉；
- 配置号幂等 + 过期传输安全丢弃：以 `installedCfgNum[s]` 记录最近成功安装的迁移配置号，重复/迟到同号或旧号仅合并不重复计时；过期丢弃仅在本组"不拥有"该分片时进行。

---

## 3. 核心模块实现要点

### 3.1 Raft 共识层
- **选举（2A）**：随机化选举超时（260–480ms），`RequestVote` 带 `LastLogTerm/LastLogIndex` 新旧比较；
- **日志复制（2B）**：`AppendEntries` 一致性检查 + 基于 `ConflictTerm/ConflictIndex` 的快速 `nextIndex` 回退；`advanceCommit` 仅提交当前任期日志（Raft 提交安全性）；
- **no-op 任期首条**：leader 上任立即追加空命令，借助"当前任期"条目间接拉动旧任期未提交日志；
- **持久化（2C）**：`persist()` 保存任期/投票/日志/快照边界，KV 层 `init()` 中 `gob.Register(Op{})` 避免重启反序列化失败；
- **快照（2D）**：`Snapshot/CondInstallSnapshot/InstallSnapshot` 供落后节点追赶；
- **Pre-Vote 预投票（#44）**：候选人正式自增任期前先以意向任期征求多数派预投票，日志落后/分区节点拿不到多数预投票，永不抬任期扰动稳定 leader，显著降低网络抖动下的无效重选与尾延迟；
- **LeadershipTransfer（#45）**：`TimeoutNow` RPC + `LeadershipTransfer(target)` 实现平滑换主，用于负载再平衡与计划内维护；
- **leader lease（本轮新增）**：维护 `lastContact[peer]`，`HasLeaderLease()` 统计选举窗口内联系到多数派的节点，仅持 lease 时对外提供 ReadIndex 线性一致读。注意：HasLeaderLease 仅表示上任瞬间已接触到多数派，并不保证本任期 no-op 已提交、commitIndex 已拉动覆盖旧 leader 已提交写；真正的快路径读还需 HasCommittedCurrentTerm 双守卫（见 3.4 与第 4 节 I195）。
- **raft 层不变式守卫（I196）**：I195 的 ShardKV 快读正确性完全依赖 raft 层 `committedCurrentTerm` 语义——新 leader 重提交本任期 no-op 前 `committedCurrentTerm` 必须恒为 false、no-op 提交后 `commitIndex` 必须拉动覆盖旧 leader 已提交写。本轮回在 raft 层新增白盒回归 `TestReadIndexLeaseRequiresCommittedCurrentTerm`：在真实 3 节点集群上锁定两条不变式——(1) 杀主重选后必观测到 `HasLeaderLease()=true && HasCommittedCurrentTerm()=false` 的选举抖动窗口（3/3 运行均抓到，证明 I195 守卫防护的是运行时真实存在的窗口）；(2) 新 leader 提交当前任期后 `commitIndex >= 旧 leader 稳定 commitIndex`（拉动保证）。任何对 `advanceCommit/becomeLeader/persist` 的改动若误把 `committedCurrentTerm` 提前置 true、漏重置或误持久化，都会在 raft 层暴露，从而在共识层（而非仅 ShardKV 层）兜住 I195 窗口被悄然 reopen。

### 3.2 KV 层（Lab 3）
- `Op` 携带 `ClientId+Seq`，applier 幂等去重；`Clerk` 轮询遇 `WrongLeader` 自动切换 `leaderHint` 重试；
- **Clerk 指数退避（#46）**：固定 50ms 改为 10ms→翻倍→500ms 上限，减少迁移/换主窗口下的重试风暴；
- **客户端会话 GC**：`sessions[clientId]` + 后台 TTL 清理，避免长期运行内存无限增长；
- **Get ReadIndex 快路径**：leader 持 lease 时以 `commitIndex` 为一致性点、待状态机 apply 后直接本地读，省一次日志追加。

### 3.3 ShardMaster（Lab 4 控制器）
- 维护 `NShards=10` 分片映射，每次配置变更经 Raft 复制；
- **确定性最小搬动再平衡**：保留上一版映射，仅把多余分片从最重组搬到最轻组，避免全量重排引发大范围迁移抖动；
- **Move 只改一个分片**：新配置继承完整映射、仅覆盖目标分片，防止其余分片被清零成"未分配(gid 0)"导致集体失主卡死（最隐蔽的一类 bug）；
- 输入校验（空 group / 重复 gid / 越界分片 → `ErrInvalid`）直接拒绝不落 Raft。

### 3.4 ShardKV（Lab 4 数据面）
- **串行推进配置**：`pollConfig` 仅当本组无未决迁移且最新配置恰好领先 1 步时推进 `NewConfig`，避免迁移重叠竞态；
- **跨迁移客户端幂等**：每个分片独立保存 `LastSeq/LastResult`，迁移前后命令不重复执行；
- **快照压缩**：KV 状态（各分片数据 + 当前/上一版配置 + 迁移中 incoming/pending）gob 压入 Raft 快照；
- **ReadIndex 线性一致快读（I195 双守卫）**：`Get` 快路径需同时满足 `HasLeaderLease()` 与 `HasCommittedCurrentTerm()`——仅当本任期 no-op 已提交、`commitIndex` 已拉动覆盖所有先前已提交写后才以 `commitIndex` 为一致性点本地读；否则回退常规 `propose`。单一 `HasLeaderLease()` 会因"上任瞬间即得票即算多数派接触"而在状态机尚未回放已提交写时读到空值（杀主+重选窗口偶发"写已提交却读到空"），I195 以双守卫彻底消除。
- **迁移 liveness 看门狗（cycle 39）**：`pollConfig` 在 `pendingIn/pendingOut` 任一分片卡滞 >3s 时以最新配置重拉/重推，并 `config_stalls` 指标可观测。

### 3.5 全栈与可观测性
- **`gateway`**：`GET/PUT/POST-append /kv/{key}` + `/healthz` + `/metrics`（按 `Accept` 协商 Prometheus 文本 / JSON）+ `/debug/shards`（分片归属 + 迁移状态 + 卡滞时间戳）+ `/status`（集群健康总览）+ `/debug/*`（routes/version/config/log/accesslog/groups/raft/migrate 等）；冻结时"1s RPC 超时 + 5s 有界重试"快速失败返回正确 REST 语义（409/503/504）而非挂死；并发限流（默认 64，超量 429）、优雅退出 `Shutdown(ctx)`；
- **`kvcli`**：HTTP 客户端 + `bench [op] [ops] [workers]` 报告吞吐与 p50/p95/p99 延迟；
- **`metrics`**：零依赖并发安全指标库（Counter + Gauge + 有界环形 Histogram + Registry），纯原子增量接入 raft/kvraft/shardkv 热路径；
- 网关中间件（分级结构化日志、`X-Request-ID` 透传、每客户端令牌桶限流、CORS、YAML 配置加载、`/readyz` 就绪探针、`http.TimeoutHandler` 兜底）一律 **cluster-free 单测**（`&Server{}` + no-op handler + `httptest`），规避沙箱 raft 选举偶发挂死。

---

## 4. 关键技术难点与解决方案

1. **4 类真实并发缺陷（Lab 4 迁移/快照热路径）**
   - `InstallShard` 深拷贝迁移数据，消除 `concurrent map read and write`；
   - `installSnapshot` 不在已持锁下嵌套加锁，消除 `maxraftstate>0` 下死锁；
   - `Clerk.config` 在 `ck.mu` 内捕获快照，消除 `-race` 下读写竞态；
   - `ShardKV/ShardMaster` 新增 `killCh`，`Kill()` 后 applier 及时退出，消除 goroutine 泄漏。

2. **3 组/多跳再平衡冻结（原最高优先级未解风险，现已根治）**
   - 失效路径① fetch 侧卡死：`applyNewConfig` 消费 `incoming` 必清 `pendingIn` + `pollConfig` 仅以 `pendingIn` 门控配置推进 + `migratePump` 保活泵消除；
   - 失效路径② orphan incoming（中间组收到迟到推送却无主可装、真主永远拉不到）：改由 `GetShard` **安全回退**（本组 `shards[s]` 缺失时返回 `incoming[s]`，只读、零改写、零新协程，与权威路径正交）消除；
   - 曾 3 次尝试"独立 goroutine 改写状态式"修复均因状态冲突 / RPC 风暴回归，`GetShard` 回退为正确解；
   - 结果：`TestSKVReMigration`（A→B→A 漂移）与 `TestSKVThreeGroupChurn`（3 组 churn）现已**常驻且确定性通过**（`-count=3` 串行复跑 3/3），CI 另设 `migration-stress`（`-count=5` 高压）防回归。

3. **性能瓶颈认知（避免客户端侧伪优化）**
   - 基准 ~16.6 ops/sec、~52% 操作出错，根因是 `Clerk.refresh()` 每次 op 都查 ShardMaster + 50ms 重试睡眠；
   - 已验证 `Clerk` config 缓存 / leader-preference / fetchShard 回源等客户端微优化均无效（扩大 stale 窗口或仍 flaky），证实瓶颈在 Raft 序列化 + 重试，修复必须在服务端迁移状态机——这一结论直接指导了第 2 点的根治方向。

4. **选举抖动窗口线性一致读守卫（I195）**
   - 现象：`Get` 的 ReadIndex 快路径原仅判 `HasLeaderLease()`；新当选 leader 在"本任期 no-op 提交前"其 `committedCurrentTerm` 恒为 false、状态机尚未回放已提交写，而 `HasLeaderLease()` 因选举得票即算"多数派接触"、上任瞬间即为 true，于是快路径基于过时偏低的 `commitIndex` 读到尚未 apply 的空状态机、返回空值（杀主 + 重选窗口偶发"写已提交却读到空"）。
   - 修复：快路径加 `HasCommittedCurrentTerm()` 双守卫，必须等本任期 no-op 提交、`commitIndex` 拉动覆盖所有先前已提交写后才走本地快读，否则回退 `propose` 经共识返回真实值。
   - 验证：新增命名回归 `TestLeaderKillReadNoStaleAfterElect`（结构等同 `TestLeaderKillRecover`，16 轮杀主→重选后立即读，断言绝不返回空）——修复前该窗口偶发 Fatalf，修复后确定性绿；`TestLeaderKillRecover` 同步加固。

5. **极端 churn 下配置归属发散（已知局限，I197）**
   - 现象：在 `TestLinearizableUnderChaos`（3 组 3 副本真实集群 + 每 400ms 杀全组 leader + 迁移 churn + 并发读写）下，单写者每键线性一致检查器 `checkSingleWriterPerKey` 抓到读返回空值违规。诊断证据：失败读的服务端 leader 其 `logLen == commitIdx == applied`（日志仅含配置+迁移条目、零客户端写），且 16 个写（含 `Put P0` 起）已全部在 `r.invoke` 前 commit。
   - 根因：写者/读者 `Clerk` 对分片归属的**配置视图不一致**——写落在「陈旧 owner 组」（其 `kv.config` 也滞后，`applyCmd` 不返回 `ErrWrongGroup` 而直接应用并返回 `OK`），真正当前 owner 的对应分片始终为空，读者命中当前 owner 即读到空。属 Lab4 配置一致性专项（Clerk 周期重查最新 config + 不拥有分片时写返回 `ErrWrongGroup`）。
   - 处置：**已知局限，非稳态缺陷**。该违规仅在「每 400ms 杀全组 leader + 迁移 churn」的极端压测窗口下复现，稳态配置收敛路径不受影响；且 `restartReplica` 复用同一 persister（日志不丢）、`applyCmd/applyNewConfig` 逻辑正确、`persist` 不持久化 `lastApplied` 但重启会重放 `1..commitIndex`，根因是配置归属发散而非重启丢状态。仓库内已新增单写者每键线性一致检查器（`src/shardkv/linearizability_test.go`，`TestLinCheckerSynthetic` 确定性通过、`TestLinearizableUnderChaos` 默认 `t.Skip`）作为**永久回归护栏**；手动复现：`go test ./src/shardkv/ -run TestLinearizableUnderChaos -count=1 -v -timeout 90s`。

---

## 5. 测试、覆盖率与性能验证

**测试矩阵（Labs 2–4 确定性用例全绿，重复运行稳定）**
- `raft`：选举 / 复制 / 持久化 / 快照共 8 用例；
- `kvraft`：基础 / 并发 / 故障转移 / 持久化 / 快照压缩 / 快照压力 6 用例；
- `shardmaster`：Join / Leave / Move / Query 及组合下的线性一致；
- `shardkv`：**10 个集成用例 + 3 个白盒用例**（`TestShardDataCopy`/`TestMergeShardData`/`TestApplyInstallShardIdempotent` 驱动迁移纯函数），含 `TestSKVLinearizableAppend`（追加顺序敏感线性一致守卫）、`TestSKVPersistRestart`（同一 Persister 重启整组副本）、`TestSKVThreeGroupChurn`（3 组 churn 根因复现载体，现已常驻）；新增 `linearizability_test.go`：单写者每键线性一致金标准检查器 `checkSingleWriterPerKey` + 确定性合成验证 `TestLinCheckerSynthetic` + 默认跳过的 chaos 复现器 `TestLinearizableUnderChaos`（抓 I197 配置归属发散）；
- **已知时序敏感项（非正确性格栅回归）**：`TestChaosSwingWriteDataLoss` 为高压力 swing 混沌测试，隔离运行（`-run` 单测，-count=1 连跑 6 次）确定性通过；仅在全包并发 `go test -cover ./...`（19 包同时抢 CPU）时偶发触碰其内部 swing deadline 而 FAIL。该现象在剔除 I195 的 HEAD 版本上同样复现（对照验证），属测试自身并发负载健壮性待加固，不影响核心正确性结论。
- **混沌测试（I16/I18）**：迁移窗口内反复杀主并重启、长轮次崩溃-重选，守护崩溃恢复路径不复发；CI 侧 `chaos-race`（`-race` 覆盖混沌/迁移）、`chaos-long`（放大轮次）双 job。

**覆盖率**：全量 `go test ./...` 覆盖率 **74.2%**（cluster 93.2 / demo 93.0 / kvraft 84.4 / metrics 84.7 / shardmaster 76.5 / raft 77.8 / shardkv 66.5 / gateway 66.7 / kvcli 54.1）。

**性能**：`BenchmarkSKVPutGet` / `BenchmarkSKVGet`（8 并发、5s）打印 ops/sec 与延迟分位数；Clerk 指数退避 + leader 缓存后迁移 churn 下客户端收敛更快、RPC 更少。

**CI**：GitHub Actions（ubuntu + gcc）跑 `-race`、非阻断 `lint`（golangci-lint）、`coverage` 上传 artifact、全栈冒烟、混沌/迁移压力 job；本地无 gcc，靠高频 churn + 多轮循环测试暴露竞态。

---

## 6. 自主迭代开发过程

项目采用 `self-driving-dev` 机制自主迭代演进，关键阶段：
- **R1（Lab 4 半成品修并发 bug + 补测试）**：cycle 1–13 修 4 类并发缺陷、建测试矩阵、定位 3-group 脆弱性；
- **R2（#44–#53）共识层 + 网关双增强**：Pre-Vote、LeadershipTransfer、Clerk 退避、网关分级日志 / `X-Request-ID` / 令牌桶 / CORS / YAML 配置 / 调试端点，34 commits 快进推送；
- **R3（#54–#103）网关安全 / 稳健 / 可观测**：请求体上限、gzip、安全头、IP 白名单、`/debug/*`、健康熔断、按路由限流（修 tokenBucket data race）；
- **R6/R7（#177–#223）弹性 / 传输层 / 正确性双修 / 可观测收口**；
- **人工主导迭代（I1–I20）**：真部署化收口 + 混沌挂死根因深啃（含 3-group 冻结根治、GetShard 安全回退、配置号幂等）。

迭代纪律固化：默认只本地提交、不 `--force`、不 `rm -rf`；仅在授权时快进推送；网关新功能一律 cluster-free 测试；`.github/workflows/*` 首次推送需 `workflow` scope。

---

## 7. 总结与展望

本项目完整实现了 MIT 6.824 风格的 Raft + 容错 KV + 分片 KV 系统，并工程化为一套可运行、可观测、可压测的全栈服务。实训过程中最关键的认知是：**分布式系统的正确性必须靠"服务端状态机不变量 + 确定性测试 + 混沌复现"三重保障**，客户端侧微优化往往是死路；而 3-group 再平衡冻结这类"间歇性挂死"问题，唯有回到异步迁移与配置推进的竞态根因、用"只读安全回退"替代"状态改写"才能根治。

后续可深化方向：真实 gRPC/HTTP 传输层替换 labrpc 使组件可独立部署；shardkv 覆盖率补强（pendingIn/pendingOut 冻结自愈黑盒用例）；响应缓存与去重进一步优化吞吐；将 `self-driving-dev` 的迭代经验沉淀为可复用工程方法；极端 churn 下配置归属发散（I197 已知局限）的根治——Clerk 周期重查最新 config 与越界写返回 `ErrWrongGroup`。

---

## 致谢

感谢课程提供的 MIT 6.824 实验框架与评测用例，为分布式共识与分片系统的正确性格栅提供了基准；感谢 `self-driving-dev` 自主迭代机制在长周期工程演进中对并发缺陷、迁移冻结与可观测性的持续巡检。本报告所有技术事实以仓库当前状态（`README.md` / `docs/` / 测试矩阵）为准。

> 说明：本文件为实训报告草稿，技术事实以仓库当前状态（`README.md` / `docs/` / 测试矩阵）为准；提交前请学生按所在课程格式要求补充封面、摘要、致谢等章节，并复核具体数值（覆盖率、基准吞吐）与本地实测一致。
